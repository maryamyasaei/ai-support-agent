import { searchDocsTool } from "./searchDocs";
import { createTicketTool } from "./createTicket";
import { getUserContextTool } from "./getUserContext";

export const allTools = [searchDocsTool, createTicketTool, getUserContextTool];

export { searchDocsTool, createTicketTool, getUserContextTool };
